# 2026-08-22

## 个人中心弹窗头像/改名
- 新建 src/components/tw-profile-center-modal/（profile-center-modal.jsx + css），复用反馈区 flash 动画与默认头像。
- 接入 menu-bar.jsx 替换原 placeholder。
- 支持头像本地/云端同步：新增 netlify/functions/avatar.js、tw-auth-store.js 的 getAvatar/setAvatar，Supabase email_accounts.avatar_url。
- 输入框改用 scratch-gui 原生 Input 组件，按钮使用主题色 -secondary。
- 修复 CSS 未定义变量（-white-blur/-accent 等）。
- 所有文案 defaultMessage 英文化，generated-translations.json zh-cn/zh-tw 补中文，支持中英文切换。

## 反馈系统安全加固（晚）
- 评论鉴权重做：add_comment 须设备密钥、owner_id 服务端可信写入；delete_comment 双鉴权（帖子作者/评论作者）。
- status 列并入主结构授权，与增量文件解耦。
- SQL 旧/废弃文件（4 个）移入 sql-archive/，根目录仅留 tw-db-schema.sql + tw-admin-role.sql。
- 图片上传收口到服务端 upload-image.js：删 Storage anon INSERT 策略；校验 token→device_id、字节魔数类型、≤5MB、每日每设备上限（image_uploads 表 + can_upload_image 函数）；前端 uploadImage 改 POST 到 /.netlify/functions/upload-image（带 x-tw-token 头）。
- 每日发帖上限改按设备终身 ID 计数：submit-feedback.js 从 token 取 device_id，调 can_submit_feedback(submitter_owner_id)；入库 owner_id 强制用 token 的 device_id，改名无法绕过。
- 更新日志 v2026.8.23 已追加以上条目。
