# 2026-08-25

## TwT 登录弹窗底部链接对齐修复
- **问题**：登录页底部"还没有账号？去注册 或 忘记密码"文字不在同一水平线，"还没有账号？去注册"偏下。
- **原因**：`.switchHint` 全局样式有 `margin-top: 0.75rem`，影响到底部链接区域的第一个元素。
- **修复**：在 `.bottomLinks` 内覆盖 `.switchHint { margin-top: 0; }`。
- **文件**：`src/components/tw-login-modal/login-modal.css`

## TwT 邮件验证码 Logo 不显示修复
- **问题**：预览 HTML 和 send-code.js 中的 logo base64 被截断（1952字符），导致邮件中 logo 无法渲染。
- **原因**：之前的 base64 从 workbuddy blob 中提取时被截断。
- **修复**：从完整的 `cat_logo.svg` 文件重新生成 base64（14924字符），同时更新预览 HTML 和生产代码。
- **文件**：`twt-email-preview.html`、`netlify/functions/send-code.js`
