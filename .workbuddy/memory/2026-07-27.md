# 2026-07-27

## 更新日志标签加图标
- 需求：给更新日志的分类标签（新增/改进/修复）增加图标。
- 实现（单一数据源，两处渲染复用）：
  - `src/playground/changelog/changelog-data.js`：原 `TAG_ICON` 为 emoji（✨🔧🐛）。**后用户要求不使用 emoji**，改为内联 SVG 路径数据：`{new:'M11 5h2v6h6v2h-6v6h-2v-6H5v-2h6V5z'(加号), imp:'M12 4l7 7h-4v9h-6v-9H5l7-7z'(上箭头), fix:'M14.7 6.3a4 ...'(扳手)}`（24x24 viewBox 的 path `d`）。
  - 两处渲染改为 `<span className={tagIcon}><svg viewBox="0 0 24 24"><path d={TAG_ICON[tag]} /></svg></span>`；CSS 的 `.tagIcon` 改 `inline-flex` 且 `.tagIcon svg { width:1em; height:1em; fill:currentColor; }` 随标签字号/颜色自适应。
  - 注意：弹窗里 `imp` 标签本就因 `change.tag !== 'imp'` 不渲染（历史逻辑保留），故改进图标只在独立页出现；当前 changelog 仅一条 `fix`，两处均显示扳手 SVG。
- 验证：`changelog.js` 含加号路径、`player.js` 含扳手路径、emoji 计数 0（已清除）。dev server 已重编译。
- 易退回：✅（删 TAG_ICON 引用与 CSS 即可）；危害🟢低。
- 注：Bash 在本环境对单引号/heredoc 不稳定；验证类命令改用 Write 写 .py（sandbox fs，与 dev server 共享）再 `python _verify_icons.py` 最稳，避免 `rm` 触发 sandbox bypass 导致文件不可见。

## 日志文案调整（同日早些）
- 用户要求 `v2026.7.26` 只保留「将「音频编码器」扩展改为本地同源加载。」一条，标签由 `imp` 改为 `fix`，并去掉「不再依赖远程 Netlify 地址。」后缀。已按原话定稿。
