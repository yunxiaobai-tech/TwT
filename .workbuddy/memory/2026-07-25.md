# 2026-07-25

## 熟悉 TwT 项目代码结构
- 通读 package.json、README、webpack 入口、探索 agent 梳理了整体架构。
- 确认「更新日志」机制：单一数据源 `src/playground/changelog/changelog-data.js`（`CHANGELOG_DATA`，倒序），被 `changelog.html` 与播放器页脚 Changelog 弹窗复用。后续每次改代码需更新此文件（改动过小可跳过）。
- 记录了 TwT 关键入口、Redux 组织、已有 tw-* 定制点到 MEMORY.md。
- 关键路径：挂载 `src/playground/app-target.js`；editor 入口 `editor.jsx`；player 入口 `player.jsx`；布局/Footer `render-interface.jsx`；GUI 容器 `containers/gui.jsx`。
