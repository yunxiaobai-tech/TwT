# 2026-08-24 工作日志

## 忘记密码功能完整实现
- 前端：login-modal.jsx 新增 forgotStep 状态机（none → email → code）
- 后端：reset-password.js（scrypt 哈希 + 踢出所有设备登录态）
- 邮件：send-code.js 模板重新设计（SVG logo base64 内联到问候语左侧）

## Bug 修复记录
1. CSS `.separator` 颜色继承问题 → 显式设置 `$text-primary` + `opacity: 0.4`
2. 重置按钮硬编码中文 → 改用 `T('resetBtn')` / `T('resetting')`
3. 语言切换 tooltip 硬编码 → 改用 `T('switchLangTitle')`

## Git 提交历史
- 9817690 feat: 忘记密码功能 + 邮件模板重新设计（logo内联）
- 3ae0e92 fix: 修复硬编码中文字符串（国际化）
